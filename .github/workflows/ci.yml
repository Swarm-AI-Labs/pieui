name: CI

on:
    push:
        branches: [main, develop]
    pull_request:
        branches: [main, develop]

jobs:
    step-suites:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Setup Bun
              uses: oven-sh/setup-bun@v1
              with:
                  bun-version: '1.3.12'

            - name: Install dependencies
              run: bun install --frozen-lockfile

#            - name: Run step suites with logs
#              shell: bash
#              run: |
#                  mkdir -p test-artifacts
#                  set +e
#
#                  bun run test:step1 2>&1 | tee test-artifacts/step1.log
#                  step1_status=${PIPESTATUS[0]}
#
#                  bun run test:step2 2>&1 | tee test-artifacts/step2.log
#                  step2_status=${PIPESTATUS[0]}
#
#                  bun run test:step3 2>&1 | tee test-artifacts/step3.log
#                  step3_status=${PIPESTATUS[0]}
#
#                  bun run test:step5 2>&1 | tee test-artifacts/step5.log
#                  step5_status=${PIPESTATUS[0]}
#
#                  {
#                    echo "step1=${step1_status}"
#                    echo "step2=${step2_status}"
#                    echo "step3=${step3_status}"
#                    echo "step5=${step5_status}"
#                  } | tee test-artifacts/step-status.txt
#
#                  if [ "${step1_status}" -ne 0 ] || [ "${step2_status}" -ne 0 ] || [ "${step3_status}" -ne 0 ] || [ "${step5_status}" -ne 0 ]; then
#                    exit 1
#                  fi

            - name: Build package
              run: bun run build

#            - name: Run step4 build/export checks with logs
#              shell: bash
#              run: |
#                  mkdir -p test-artifacts
#                  set +e
#
#                  bun run test:step4 2>&1 | tee test-artifacts/step4.log
#                  step4_status=${PIPESTATUS[0]}
#
#                  echo "step4=${step4_status}" | tee -a test-artifacts/step-status.txt
#
#                  if [ "${step4_status}" -ne 0 ]; then
#                    exit 1
#                  fi
#
#            - name: Cleanup temporary test artifacts
#              if: always()
#              run: bun run test:cleanup

            - name: Upload step-suite logs
              if: always()
              uses: actions/upload-artifact@v4
              with:
                  name: step-suite-logs
                  path: test-artifacts
                  if-no-files-found: ignore
